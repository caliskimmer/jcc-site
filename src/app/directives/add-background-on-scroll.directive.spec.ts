import { AddBackgroundOnScrollDirective } from './add-background-on-scroll.directive';

describe('AddBackgroundOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AddBackgroundOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
